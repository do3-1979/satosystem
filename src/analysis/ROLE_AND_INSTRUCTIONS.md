# 役割と運用指示 (GitHub Copilot エージェント)

最終更新: 2025-11-16

## 役割
- 本エージェントは Python 暗号資産トレードボットのアーキテクチャ改善・戦略検証・品質向上を支援する。
- コードリーディング結果と改善提案を継続的に集約し、重複解析を避けるために `analysis/` 以下へ中間成果物を保存する。
- 以後の解析では、まず既存ファイル (本ファイル / module_map.json / PROJECT_ANALYSIS_*.md) を参照し差分のみ追加する方針とする。

## 基本方針
1. 既存成果物を再利用し、再読込が必要な場合のみ対象ファイルを限定して読む。
2. 追加調査時は `PROJECT_ANALYSIS_YYYY-MM-DD.md` を日付単位で新規作成し、既存と重複する部分は「参照: 過去分析」と明記。
3. 変更点が生じたモジュールは `module_map.json` の該当セクションのみ更新 (全体再生成は避ける)。
4. 戦略ロジックやリスク計算の挙動差分は `strategies/CHANGELOG.md` 等へ分離を検討 (未作成)。

## 成果物一覧
- `ROLE_AND_INSTRUCTIONS.md` : 本ファイル。役割・運用ルール。
- `module_map.json` : クラス/関数/依存関係のマップ (静的解析ベース)。
- `PROJECT_ANALYSIS_2025-11-16.md` : 初期包括分析。以後は増分方式。

## 更新手順
1. 新規分析を行う前に既存 `module_map.json` を確認。
2. 新規モジュール追加時は差分部分のみ JSON に挿入。
3. 改善提案を実装したら `IMPROVEMENTS` セクションへ記録し、状態(提案 / 実装済 / 保留)を明示。
4. 実運用用とバックテスト用の差異が拡大した場合は `runtime_modes.md` を別途作成。

## 留意事項
- ログ・バックテスト結果など肥大化する成果物は原則集約/圧縮済み領域 (`logs/`) を参照。解析側にはメタ情報のみ記載。
- シングルトン利用箇所 (Logger, PriceDataManagement) の状態共有に伴う副作用へ注意する。
- 売買方向表記の不統一 (外部API: 'buy'/'sell', 内部: 'BUY'/'SELL') は設計上の整流候補。

### データキャッシュとVCS除外方針 (C6)
- キャッシュDB: `src/ohlcv_data/ohlcv_cache.db`（SQLite, OHLCVローソク足）
  - 互換ファイル: `src/ohlcv_data/ohlcv_cache.db-wal`, `src/ohlcv_data/ohlcv_cache.db-shm`
- 生成場所: `src/ohlcv_data/` 配下（自動生成）。
- バージョン管理: いずれもVCS除外（`.gitignore`で `src/ohlcv_data` ディレクトリごと除外＋DB/WAL/SHMを明示）
- 目的: 過去取得分の再利用とAPI/IO削減（欠損範囲のみ追加取得しマージ）。
- 参照: 価格初期化は `price_data_management.initialise_back_test_ohlcv_data` が `OHLCVCache` を使用。
- 注意: 解析共有時はDBをコミットせず、必要なら再生成すること（バックテスト実行で自動作成）。

#### タイムフレーム区別
- 同一テーブル `candles` 上で `timeframe` (分) 列により 1分足(1) / 2時間足(120) を分離。
- 主キー `(symbol, timeframe, close_time)` により衝突なく共存。
- 現状 2時間足は API 直接取得。将来最適化: 1分足から集約再構築し API 呼び出し削減。
- 既存期間が所定本数揃っている場合 `has_sufficient_cache` 判定で再取得スキップ (許容差 2 本)。
- 欠損ギャップ未検出: 本数一致でも内部欠損があり得る。完全性向上時はギャップ検出導入予定。

## 次回以降の利用手順 (テンプレ)
```
1. analysis/ を確認
2. module_map.json の対象モジュールから再解析必要性を判定
3. 差分抽出 → PROJECT_ANALYSIS_<新日付>.md 作成
4. 必要なら module_map.json 部分更新
```

## 課題トラッカ (ハイレベル + 旧ID参照)

以下は抽象レイヤで再編したロードマップ課題。旧 granular ID (A1〜A9) は各カテゴリ内にマッピングし継続参照。

### マクロ目標 (M 系列)
- M1: リポジトリ差分完全整合 (分析成果物とコードの食い違い解消 / 継続同期プロセス確立)
- M2: `Readme.md` 全面刷新 (現状破棄→アーキ/導入/戦略/運用/改善ループを明記)
- M3: リファクタリングバックログ運用 (抽象→具体Issue化テンプレ、優先度基準定義)
- M4: 戦略改善ループ確立 (バックテスト→指標→改善→再検証→採用判定)

### カテゴリ課題 (C 系列)
- C1 アーキテクチャ整合 / オブジェクト契約 (旧A1: `Order` コンストラクタ vs 利用側引数順序不整合)
- C2 命名 & 標準化 (旧A2: `side` 表記統一 buy/sell → BUY/SELL or lower-case 正規化)
- C3 耐障害性 & 例外処理 (旧A3: `Bot.run` try/except 未実装 + 復旧/再試行ポリシー)
- C4 設定アクセス性能 (旧A4: `Config` の多頻度クラスメソッド呼び出し → キャッシュ/インスタンス化)
- C5 イベント駆動拡張 (旧A5: `Event` 未統合 → 発火ポイント/購読API設計)
- C6 パフォーマンス最適化 (旧A6: 価格取得/インジ計算頻度最適化 + lazy更新)
- C7 バックテスト & レポーティング品質 (旧A7: 終了条件/結果集約/指標(Sharpe, 最大DD)出力強化)
- C8 リスクロジック統合 (旧A8: Stop値算出重複/コメントTODO整理→単一リスク計算モジュール化)
- C9 分析 & ログパイプライン汎用化 (旧A9: ログ→集計パイプライン抽象化 / メタ統計生成)

### ステータス管理指針
状態は `提案 | 調査中 | 実装中 | 実装済 | 保留` を基本。粒度の細かいタスクは Issue へ分離しカテゴリIDをラベル付与。

### 現在ステータス (2025-11-16 M1 コミット完了)
- **M1: 実装済** ✅ (commit cf1a7d5)
  * リポジトリ同期完全化
  * module_map / PROJECT_ANALYSIS / 役割ドキュメント整備
  * バックテスト実行確認済 (4m 6s、20,040 サンプル)
  * メトリクス出力動作確認 (JSON サマリ生成)

- **M2: 実装済** ✅ (commit cf1a7d5)
  * Readme.md 全面刷新 (139行→195行)
  * アーキテクチャ概要・セットアップ・ワークフロー追加
  * メトリクス出力フォーマット明記
  * コントリビューションガイド追加

- **M3: 実装済** ✅ (commit cf1a7d5)
  * Issue テンプレ (docs/ISSUE_TEMPLATE_REFAC.md) 作成
  * C体系の優先度基準定義
  * 粒度細分化テンプレート確立

- **M4: 実装済** ✅ (commit cf1a7d5)
  * metrics.py 完全実装 (Sharpe/DD/PF/WinRate)
  * Bot.py メトリクス統合 (compute_metrics 呼び出し)
  * JSON サマリ出力 (backtest_summary_<timestamp>.json)
  * 損益履歴・取引結果トラッキング機能

- **C3: 実装済** ✅ (commit daa8fb3)
  * Bot.run 全体の例外処理追加、戦略/注文/リスク更新の各所をガード
  * 短期パリティテストで挙動差分なし、拡大期間でもエラーなし
- **C8: 実装済** ✅ (commit daa8fb3)
  * PSAR/ADX/ポジションサイズ計算の堅牢化（空配列/None/ゼロ除算ガード）
  * 短期パリティテストで挙動差分なし
 - **C1/C2/C5: 実装済** ✅ (branch `nextarch`, pending PR)
  * C2 命名統一: `src/side.py` に `Side(BUY/SELL/NONE)` と `normalize_side`, `to_exchange_side` を追加
  * C1 契約整合: `src/order.py` のコンストラクタを `(symbol, side, quantity, price=None, order_type='limit')` に統一し side を正規化
  * C5 イベント: `src/event.py` に `EventType` と `EventBus` を追加、`src/bot.py` から主要ポイントで `emit` 連携
  * 検証: 同期間バックテスト (2025/10/26–11/4) 実行、取引5・PF 0.26・Sharpe -1.205・PnL -51 と従前結果と整合、例外なし
 - **C4/C6/C7/C9: 提案→調査中**
  * 優先度: (1) C6/C7 (性能/レポート) → (2) C4 (設定キャッシュ) → (3) C9 (ログ/分析パイプライン)
 - **統合作業:** `nextarch` にコミット/プッシュ済（`daa8fb3`, 追加コミット `8e6e543`）。PRは未作成（依頼により中止）。

## IMPROVEMENTS (2025-11-16)

- 【C3 例外処理】状態: 実装済 (commit daa8fb3)
  - 変更: `src/bot.py` のメインループ、戦略実行、注文実行、リスク更新を try/except で保護。
  - 検証: 2025/11/1–11/4 のパリティテストで取引0・エラーなし。拡大期間(2025/10/26–11/4)でも例外無しを確認。
- 【C8 リスク堅牢化】状態: 実装済 (commit daa8fb3)
  - 変更: `src/risk_management.py` の PSAR/ADX/停止値・ポジションサイズで空配列/None/ゼロ除算を防止。
  - 検証: 2025/11/1–11/4 のパリティテストで挙動差分なし。
 - 備考: `src/config.ini` は `.gitignore` 管理で VCS から除外。秘密鍵は無効化・再発行済み。

   - 【C2 命名統一】状態: 実装済 (branch `nextarch`)
    - 変更: `src/side.py` を新規追加。内部表記を 'BUY'/'SELL'/'NONE' に統一し、外部API用に小文字へ変換するヘルパを提供。
    - 影響: 既存の呼び出し側は大半が大文字表記のため非破壊。変換点は `Order`/`Bot` 経由で統一。

   - 【C1 オブジェクト契約】状態: 実装済 (branch `nextarch`)
    - 変更: `src/order.py` の引数順序・デフォルト値を呼び出し側に合わせて統一。`side` は正規化して保持。
    - 影響: 呼び出し側の明示引数はそのまま動作。辞書化 `to_dict` でも正規化済み side を出力。

   - 【C5 イベント駆動】状態: 実装済 (branch `nextarch`)
    - 変更: `src/event.py` に `EventType` と `EventBus` (subscribe/unsubscribe/emit) を追加。`src/bot.py` 主要イベント (TICK/SIGNAL/ORDER/PORTFOLIO/RISK/ERROR) で `emit` 実装。
    - 影響: 既存フローは同期処理で非破壊。購読者未登録時はオーバーヘッド極小。拡張ポイント提供。

### C6 パフォーマンス最適化 Backlog (2025-11-16 追加 / 状態: 提案→保留)

現状: `src/bot.py` に PerformanceTracker を導入し以下フェーズ時間を取得済み。
  - price_update ~82% / logging ~15% / risk_update ~3% / その他 <1%
目的: ボトルネック (price_update + logging ≈96%) を非破壊・段階的に削減し、戦略ロジックのパリティ（取引数/タイミング/指標値）を維持する。

本番影響 (現実装): 1ループあたり `perf_counter()` 呼び出し ~6回 + dict加算数回。推定オーバーヘッド <0.5% でライブ運用への影響は極小。
無効化方針: 今後 `config.ini` に `enable_performance_tracking=false` を追加し計測を条件化 (未実装)。

改善タスク一覧 (順序は推奨実行順 / すべて状態: 提案 / 保留):
1. Donchianチャネル前計算キャッシュ (deque + 差分更新で最小 O(1) 化)
2. ボラティリティ (ATR/類似) の増分更新 (前足差分のみ再計算)
3. リスク更新条件化 (価格 or ポジションサイズ変化時のみ再計算)
4. ログ削減 (毎分詳細→イベント時/閾値超過時のみ)
5. ログ I/O バッファリング (まとめ書き / flush 間隔制御)
6. 長期インジ (2h足) 再計算 gating (新しい2h足確定時のみ)
7. NumPy/array化 (price系列を Python list→ndarray 化しスライス高速化)
8. 2時間足再構築 (1分足集約でAPI呼び出し削減しキャッシュ活用度向上)
9. キャッシュギャップ検出導入 (欠損ブロックのみ再取得で初期化時間短縮)
10. パフォーマンストラッキング enable/disable フラグ導入 (ライブ軽量化オプション)

検証要件 (各タスク共通):
  - バックテスト同期間リランで取引タイミング・件数・PnL・PF・Sharpe が前後一致 (誤差許容: 指標浮動小数丸め以内)
  - 計測前後で `price_update` + `logging` 合計時間が削減されること (初期目標: -30% / 二次目標: -50%)
  - エラー/例外の新規発生なし

ロールアウトフェーズ提案:
  - Phase A (安全): 1,4,5,10
  - Phase B (中リスク): 2,3,6,9
  - Phase C (構造変更含む): 7,8

備考: これらタスクは実装前に Issue 化しカテゴリ `C6` ラベル付与。優先度は "price_update 時間寄与率" と "パリティ影響リスク" の二軸で評価。

### パフォーマンストラッキング仕様メモ (2025-11-16)
フィールド: `iterations`, `phase_totals`, `phase_averages`, `phase_percentages`, `grand_total_sec`。
出力: `logs/performance_summary_<timestamp>.json` (バックテスト終了時のみ)。
ライブ時: 出力頻度過多回避のため将来は interval / manual trigger を検討。
拡張予定: CSV/Prometheusエクスポート、ヒストグラム(分位点)記録、メモリ使用量計測併設。

## 差分同期プロセス (M1 詳細)
1. `analysis/` 内成果物と `src/` コード構造を週次/変更イベント時に比較。
2. 新規/変更クラス検出時: `module_map.json` 部分更新 (全再生成回避)。
3. 戦略/リスク/設定の仕様差分は `PROJECT_ANALYSIS_<date>.md` に増分追記し過去参照リンク。
4. 乖離種別 (命名 / 引数順序 / 例外処理欠落 / ドキュ不足) をタグ化し Issue テンプレへ投入。
5. 解消後: 該当カテゴリステータスを `実装済` に遷移しコミットハッシュを記録。

## README 刷新方針 (M2 概要)
現行のクラス羅列中心構成を破棄し、以下章立てへ変更予定:
1. プロジェクト概要 / 目的 (自動トレード + 改善ループ)
2. アーキテクチャ概要 (主要モジュールとデータフロー簡図)
3. セットアップ (環境 / 依存 / APIキー / コンフィグ)
4. 実行方法 (バックテスト / ライブ / ログ)
5. 戦略開発ワークフロー (M4 詳細参照)
6. リスク管理 / 品質保証 (例外方針 / テスト / 指標)
7. ロードマップ (M/C 系列一覧)

## リファクタリングバックログ運用 (M3 詳細)
テンプレ (Issue):
```
タイトル: [C<ID>] <短い要約>
カテゴリ: C<ID>
背景: 現状問題 / 影響
ゴール: 期待される改善結果 (性能指標 / 可読性 / 安定性)
範囲: 変更予定ファイル / 非対象範囲
リスク: 破壊的変更リスク / 移行手順
検証: テストケース / メトリクス / ログ確認項目
```

優先度基準: (1) 安定性へ影響 (2) 戦略改善阻害 (3) 性能ボトルネック (4) メンテ性。

## 戦略改善ループ (M4 詳細)
1. 仮説設定: 指標 (勝率, PF, 最大DD, 日次シャープ) 目標値定義。
2. データ取得: 期間/ペア/足種を固定し再現性確保 (キャッシュ利用)。
3. バックテスト: 現行戦略 vs 変更案 (同期間) を並列実行。
4. 評価: 統計 (PnL曲線, 最大連敗, ボラ調整リターン) を比較し改善幅測定。
5. 採用判定: 最低改善閾値 (例: シャープ比 +0.2, 最大DD -10% 以上) 超過時に候補昇格。
6. 安全化: リスクユニット再計算 / 注文サイズ検証 / スリッページ耐性試験。
7. 実運用試験: 低サイズサンドボックス口座でステップ運用→問題なければ本番昇格。

## 次アクション候補 (アップデート後)
短期 (即座): 
  1. **ドローダウン計算の統一** (ログとJSONで値が乖離: 78.26 vs 54.88)
  2. **戦略リスク管理強化** (DD率 69% は過度→ポジションサイズ/ストップロジック見直し)
  3. **期末未決済ポジション処理** (バックテスト終了時の強制決済or mark-to-market)

中期: C2/C1 は実装済（nextarch, 8e6e543）。EventBus の購読活用を追加検討。  
中期: IndicatorService 分離 (PSAR/ADX/Donchian/PVO) / DataSource 抽象 (Backtest vs Live)。  
長期: 複数戦略ポートフォリオ & 動的リスク配分, ログ schema versioning。

---

## M1-M4 完了後の推奨改善パス

### フェーズ 1: 安定性強化 (リスク)
**優先度: 最高** — M4 で検出されたドローダウン問題を解決
- [x] C8 (リスク統合): Stop算出・ポジションサイズ計算の統一・堅牢化（commit daa8fb3）
- [x] C3 (例外処理): Bot.run 全体の try/except + 復旧戦略の骨子（commit daa8fb3）
- [ ] テスト: 複数期間でバックテスト再実行し DD再現性確認

### フェーズ 2: コード品質 (命名・設計)
**優先度: 高** — コードベース保守性向上
- [x] C2 (命名): side を Enum(BUY, SELL) へ統一（branch `nextarch`, commit `8e6e543`）
- [x] C1 (契約): Order コンストラクタ引数順・デフォルト値整合（branch `nextarch`, commit `8e6e543`）
- [x] C5 (イベント): Event 発火ポイント整備 (取引後/エラー時など)（branch `nextarch`, commit `8e6e543`）

### フェーズ 3: パフォーマンス (指標・分析)
**優先度: 中** — 実運用への準備
- [ ] C6 (性能): 価格取得頻度・インジ計算を lazy 更新へ最適化
- [ ] C7 (バックテスト): 複数指標の時系列ファイル出力 (PnL曲線、日次シャープなど)
- [ ] C9 (ログ): 分析パイプライン汎用化 (ログ → 自動レポート生成)

### フェーズ 4: 拡張機能 (アーキ発展)
**優先度: 低** — 長期戦略
- [ ] C4 (設定): Config キャッシュ化 + インスタンス管理
- [ ] 複数戦略ポートフォリオ & 動的リスク配分
- [ ] ログ schema versioning + 後方互換性

---
(このファイルは常に追記型で管理し、履歴は Git の diff により追跡する。)

---

## バックテスト実行ログ (2025-11-16)

**実行コマンド:**
```bash
cd /home/satoshi/work/satosystem/src && ./bot_run.sh
```

**テスト期間:** 2025/11/1 0:00 ~ 2025/11/14 23:00  
**サンプル数:** 20,040 足 (1分足)  
**実行時間:** 4分 6秒

**メトリクス結果:**
```json
{
  "total_pnl": 24.399811,
  "profit_factor": 1.311787,
  "max_drawdown": 54.877698,
  "max_drawdown_rate": 69.222278,
  "sharpe": 0.310557,
  "win_rate": 100.0,
  "trades": 8,
  "samples": 20040
}
```

**指摘事項:**
1. **ドローダウン乖離**: ログ表示では `78.26 BTC/USD` でも JSON では `54.88` → 計算方法確認必要
2. **未決済ポジション**: バックテスト終了時に SELL ポジション (`0.0313 BTC @ 97,951`) が開いたままの状態
3. **シャープ比が低い**: `0.31` は過度なドローダウンと小さい利益のバランス悪さを示唆
4. **勝率 100% は虚偽**: 取引回数 8回に対し大きなドローダウン → 未決済ポジション利益未実現の可能性

**推奨アクション:**
- [ ] バックテスト終了時に全ポジション強制決済処理を追加
- [ ] ドローダウン計算ロジック (metrics.py vs portfolio.py) の統一
- [ ] ポジションサイジング・ストップロジックのリスク見直し
- [ ] 複数期間での再テスト (月間 / 四半期 / 年間)

---

## バックテスト実行ログ (2025-11-16 追加)

**実行状況 (C3/C8 実装後の確認):**

- 期間: 2025/11/1 0:00 ~ 2025/11/4 0:00（パリティ確認）
  - 結果: 取引0、PnL=0、エラーなし（挙動差分なし）
- 期間: 2025/10/26 0:00 ~ 2025/11/4 0:00（取引発生確認）
  - 結果: 取引5、最終損益 -51、PF 0.26、Sharpe -1.205、エラーなし
  - 追加課題: ドローダウン表記の乖離、期末未決済ポジションの扱いは従前課題に継続計上

**実行状況 (C2/C1/C5 実装検証リラン):**

- 期間: 2025/10/26 0:00 ~ 2025/11/4 0:00（同期間でリラン）
  - 結果: 取引5、最終損益 -51、PF 0.26、Sharpe -1.205、エラーなし（前回と一致）
  - 生成ファイル: `logs/backtest_summary_20251116154050.json`

**実行状況 (C6 キャッシュDBパス調整・検証):**

- 変更: キャッシュDBの既定パスを `src/logs/` から `src/ohlcv_data/ohlcv_cache.db` に変更。
- 結果: 同期間バックテストで挙動差分なし（パリティ維持）。DBは `src/ohlcv_data/` に生成確認。
- VCS: `.gitignore` に `src/ohlcv_data/` および `ohlcv_cache.db`/`-wal`/`-shm` を明示的に除外。

---

## フェーズ進捗更新（チェックリスト反映）

### フェーズ 1: 安定性強化 (リスク)
- [x] C8 (リスク統合): Stop算出・ポジションサイズ計算の統一・堅牢化（commit daa8fb3）
- [x] C3 (例外処理): Bot.run 全体の try/except + 復旧戦略の骨子（commit daa8fb3）
- [ ] テスト: 複数期間でバックテスト再実行し DD再現性確認
