# 実装ロードマップ - 2025 Q4 戦略改善計画

**最終更新**: 2025-11-21  
**目的**: システム勝率回復と戦略改善の段階的実装

---

## 📋 優先度A: 🔴 分類閾値分析基盤 (Analytic Monitor)

### 背景再整理
- k2/k3 は現状トレード意思決定ロジックに未参照 → 直接勝率を左右しない。
- 役割を "事後分析 / レジーム検知" 指標へ再定義。勝率壊滅の主因として扱う記述を撤回。
- 目的: 市場レジーム変化(トレンド性/ボラティリティ構造) の早期検出と EXIT/ENTRY 改修のトリガー提供。

### 機能セグメント別進捗
| セグメント | 説明 | 進捗 |
|------------|------|------|
| 基礎分析 | 分布抽出 / 推奨算出 / ドリフト検知 | ✅ 完了 |
| 運用自動化 | 月次/四半期自動実行・レポ出力 | ⏳ 未着手 |
| レジーム分類 | ADX/ATRクラスタリング / モード判定 | ⏳ 未着手 |
| 安定化制御 | 閾値急変緩和 / 外れ値処理 | ⏳ 未着手 |
| 戦略連携橋渡し | 異常検知→EXIT/ENTRY調整トリガ | 🔄 設計中 |

現在の総合進捗目安: 約60% (≒ 基礎分析完了 / 他未実装)。

### ✅ 完了項目
1. `tools/dynamic_classification_optimizer.py` : MFE/MAE分布パーセンタイル / 推奨値 / ドリフト検知 / レポ生成
2. `tools/adaptive_threshold_monitor.py` : 最新トレードファイル検出 / 推奨適用 / 四半期レポ生成
3. 四半期順次適応検証: 推奨値変動が市場レジーム指標として機能する可能性を確認

### 🔄 設計・検討中
- 推奨値異常時に発火する "調整フラグ" の仕様 (例: TRUE TREND比率低下→EXIT再評価)
- レジーム分類の特徴量セット (ADX, ATR比率, 偏差係数, ボラティリティクラスタ)

### ⏳ 未着手
- 月次自動実行 (cron / systemd timer)
- レジーム分類アルゴリズム (k-means もしくは単純閾値ルール)
- 閾値急変緩和 (前回値とのΔ超過時段階的補間)
- A/Bバックテスト統合 (分類警告有無での戦略差異評価)

### リスケジュール (現実的分配)
```
Week 1 (11/21-11/27): 基礎分析完了 / 四半期検証 (済)
Week 2 (11/28-12/04): EXIT強化着手 (優先度B) / 分類ブリッジ仕様策定
Week 3 (12/05-12/11): レジーム分類 PoC / 自動化設計
Week 4 (12/12-12/18): 自動化実装 / 閾値安定化制御
Week 5 (12/19-12/25): 戦略連携トリガ導入 / 統合レポ
```

### KPI (分析特化)
- [ ] ドリフト検出再現率 (意図した分布変化検出) ≥ 95%
- [ ] 四半期レポート自動生成成功率 100%
- [ ] レジーム分類初期精度 (手動ラベルとの一致) ≥ 70%
- [ ] 閾値急変抑制: 1ステップ変動幅 |Δ| ≤ 25% (制御導入後)
- [ ] 戦略連携イベント → EXIT/ENTRY調整反映リードタイム < 1日

運用方針: k2/k3 は "健全性監視 / レジーム検知" のみ。直接トレード改善 KPI は EXIT/ENTRY セクションで測定。

---

## 📋 優先度B: 🟡 EXIT戦略拡張

### 背景
- **課題1**: PSAR trailing stopがレンジ相場で頻繁に刈られる
- **課題2**: 長期保持による過度なドローダウン
- **課題3**: 部分利確の効果が限定的

### 実装状況

#### ✅ 完了項目
1. **時間ベースEXIT基盤** (既存実装確認済)
   - `max_hold_bars` パラメータ (`config.ini`)
   - `trading_strategy.py` での判定ロジック
   - `bot.py` での実行処理

2. **部分利確機能** (実装済・要最適化)
   - `partial_exit_enabled`
   - `partial_exit_profit_rate` (デフォルト: 10%)
   - `partial_exit_ratio` (デフォルト: 50%)

#### 🔄 進行中
- max_hold_barsの最適値探索 (候補: 10, 20, 30)
- 部分利確パラメータのグリッド探索

#### ⏳ 未着手
- **ADX連動EXIT**
  ```python
  # 実装案
  if adx < 20 and bars_held > 10:
      decision = 'EXIT'  # トレンド弱体化で早期撤退
  ```
  
- **レンジ相場専用EXIT**
  - Bollinger Band平均回帰ロジック
  - 価格がミドルバンド±1σに到達でEXIT
  
- **ボラティリティ急縮小EXIT**
  - ATR縮小率 > 50%でEXIT
  - `atr_shrink_exit_ratio` パラメータ活用
  
- **トレーリングストップ強化**
  - 最高益からの固定%下落でEXIT
  - `max_profit_drawdown_pct` パラメータ活用

### 実装スケジュール
```
Week 2 (11/28-12/04):
  Day 1-2: max_hold_barsグリッド探索 (0,10,20,30)
  Day 3-4: ADX連動EXIT実装
  Day 5-7: レンジ相場EXIT実装

Week 3 (12/05-12/11):
  Day 1-2: ボラティリティEXIT実装
  Day 3-4: トレーリングストップ強化
  Day 5-7: 統合バックテストと最適化
```

### 成功指標 (KPI)
- [ ] Max Drawdown Rate < 50% (現状からさらに削減)
- [ ] レンジ相場での損失 -30%削減
- [ ] 平均保持期間 < 20バー (過度なホールド回避)
- [ ] ADX<20環境での勝率 > 40%

---

## 📋 優先度C: 🟢 バックテスト処理時間短縮

### 背景
- **現状**: PVOグリッド (32組合せ) で112時間 (4.6日)
- **影響**: 戦略改善のイテレーション速度が極端に遅い
- **必要性**: 週次A/Bテスト、パラメータ探索の高速化

### 実装状況

#### ✅ 完了項目
- なし (未着手)

#### 🔄 進行中
- なし

#### ⏳ 未着手
1. **並列実行機能** (multiprocessing)
   ```python
   # 実装案
   from multiprocessing import Pool
   
   def run_backtest_parallel(param_sets):
       with Pool(processes=8) as pool:
           results = pool.map(run_single_backtest, param_sets)
       return results
   ```
   - 期待効果: 8コアで理論上1/8に短縮 (14時間)

2. **キャッシュ最適化**
   - 共通OHLCVデータの使い回し (現状は毎回読み込み)
   - シグナル計算結果のメモ化
   - SQLite WALモード最適化

3. **段階的グリッド探索** (Coarse-to-Fine)
   - 第1段階: 粗い間隔 (9組合せ) → 1時間
   - 第2段階: 有望領域を密に探索 (16組合せ) → 2時間
   - 合計3時間で完了 (従来の1/37)

4. **進捗可視化ダッシュボード**
   - リアルタイム進捗表示
   - 中間結果の即時プレビュー
   - 不要な組合せの早期打ち切り

### 実装スケジュール
```
Week 3 (12/05-12/11):
  Day 1-3: 並列実行機能実装
  Day 4-5: キャッシュ最適化
  Day 6-7: 段階的探索ロジック

Week 4 (12/12-12/18):
  Day 1-2: 進捗可視化ダッシュボード
  Day 3-7: 実戦投入・チューニング
```

### 成功指標 (KPI)
- [ ] 32組合せグリッド実行時間 < 4時間 (従来の1/28)
- [ ] 並列実行効率 > 70% (理想は87.5%)
- [ ] メモリ使用量 < 2GB (複数プロセス合計)
- [ ] 週次A/Bテスト実施率 100%

---

## 📋 優先度D: 🟢 エントリー戦略改善

### 背景
- **課題1**: Keltnerフィルタ効果不明 (全12組合せで同一悪化)
- **課題2**: PVO閾値が市場状況に非適応
- **課題3**: 時間帯・出来高フィルタが弱い

### 実装状況

#### ✅ 完了項目
1. **Keltnerフィルタ実装** (トグル可能)
   - EMA期間、ATR倍率パラメータ
   - `keltner_enabled` で ON/OFF切替

#### 🔄 進行中
- Keltnerフィルタ効果の再検証 (2025年データ)

#### ⏳ 未着手
1. **ボラティリティ適応エントリー**
   ```python
   # 実装案
   if atr > atr_ma * 1.5:
       pvo_threshold_adj = pvo_threshold * 1.2  # ATR高騰時は閾値厳格化
   ```

2. **時間帯フィルタ強化**
   - アジア時間 (0-8時) vs 欧米時間 (12-20時) の勝率分析
   - `excluded_hours` パラメータの自動最適化

3. **PVO最適化 (軽量版)**
   - 直近6ヶ月データのみで検証
   - (L, T)の2次元探索 (Sは固定=5)
   - 9組合せ × 1時間 = 1時間で完了

4. **出来高確認強化**
   - 出来高急増時のみエントリー許可
   - 低出来高時のダマシ回避

### 実装スケジュール
```
Week 4 (12/12-12/18):
  Day 1-2: ボラティリティ適応エントリー実装
  Day 3-4: 時間帯フィルタ分析・最適化
  Day 5-7: PVO軽量版再最適化

Week 5 (12/19-12/25):
  Day 1-3: 出来高フィルタ強化
  Day 4-7: 統合バックテスト・効果検証
```

### 成功指標 (KPI)
- [ ] エントリー精度 (勝率) > 55%
- [ ] ダマシ回避率 > 60% (Keltner/時間帯フィルタ)
- [ ] 低出来高時のエントリー数 -40%削減
- [ ] PVO最適値の2025年適合度 > 80%

---

## 🎯 全体マイルストーン

```
2025 Q4 (11月-12月)
├─ Week 1 (11/21-27): 優先度A完了 ✅
├─ Week 2 (11/28-04): 優先度B完了 🎯
├─ Week 3 (12/05-11): 優先度C完了 🎯
└─ Week 4-5 (12/12-25): 優先度D完了 🎯

2026 Q1 (1月-3月)
├─ 統合テスト・本番検証
├─ ライブトレード段階的投入
└─ 継続的モニタリング基盤確立
```

---

## 📊 進捗トラッキング

### 現在の状態 (2025-11-21)
```
優先度A: ████████░░ 80% (モニター実装完了、初回実行待ち)
優先度B: ███░░░░░░░ 30% (基盤実装済、最適化未着手)
優先度C: ░░░░░░░░░░  0% (未着手)
優先度D: ██░░░░░░░░ 20% (Keltner実装済、効果検証中)

総合進捗: ███░░░░░░░ 32.5%
```

### 次の週のアクション
1. **今日中 (11/21)**:
   - ✅ 不要ログファイル削除
   - ✅ adaptive_threshold_monitor.py 実装完了
   - 🔄 初回チェック実行 (`--check`)
   - 🔄 効果検証バックテスト

2. **明日 (11/22)**:
   - 2025年データで再最適化
   - 推奨閾値の適用 (`--apply-recommendations`)
   - A/B比較レポート生成

3. **今週末まで (11/23-24)**:
   - max_hold_barsグリッド探索準備
   - ADX連動EXIT設計

---

## 📝 実装ガイドライン

### コーディング規約
- 新機能は `tools/` 配下に独立スクリプト化
- config.ini変更は必ずバックアップ (`*.backup_*`)
- バックテスト結果は `report/` に日時付きJSON保存
- 重要な判定ロジックは必ずログ出力

### テスト方針
- 新機能リリース前に2024年+2025年バックテスト必須
- A/B比較で既存機能を上回ることを確認
- ライブ環境投入は段階的 (1週間モニタリング)

### ドキュメント更新
- 実装完了時に本ファイルを更新
- `ARCHITECTURE_OVERVIEW.md` に設計判断を記録
- コミットメッセージに実装根拠を明記

---

**次回更新予定**: 2025-11-22 (優先度A初回実行結果反映)
